# Figure 2 and Figure 3 display refinement design

## Scope

Refine only the presentation of Figures 2 and 3 generated by
`diabetes/analysis_v2.py`. The underlying simulations, motor-unit selection,
bootstrap results, statistical tests, and manuscript wording remain unchanged.

## Figure 2

- Set the firing-rate axis range to 8–18 pps.
- Show y-axis ticks every 2 pps at 8, 10, 12, 14, 16, and 18 pps.
- Move the paired-comparison significance bracket to 17 pps so the bracket and
  significance label remain inside the new axis limits.
- Retain the randomized HD-sEMG subject estimates, their across-subject mean and
  95% BCa confidence interval, and the Normal and DPN all-active-MU truth means.
- Draw the HD-sEMG mean as a red circle with an 8-point marker, making it larger
  than the default 6-point blue subject markers while leaving the short
  confidence intervals visible; draw its 95% BCa interval and caps in red,
  above both the mean marker and subject markers.
- Draw the HD-sEMG Normal-DPN significance bracket and asterisks in red so they
  are visually associated with the red inferential summary rather than the
  green simulation truth or blue subject observations.
- Draw the mean simulation-truth segments in green.
- Preserve the existing title, labels, legend, dimensions, output path, and
  300-dpi export.

## Figure 3

- Retain the current randomized HD-sEMG median curve based on 10 eligible MUs.
- Retain the central 95% across-selection-seed band and the all-active-MU truth
  reference.
- Draw the all-active-MU simulation-truth reference as a green dashed 3-point
  line, thicker than the blue 2-point randomized HD-sEMG median curve.
- Do not display a 10-lowest-rate curve, an eligibility-only curve, a green
  eligibility comparison curve, or any lowest-rate prioritization label. The
  only green line is the simulation-truth reference.
- Preserve the existing common-random-number threshold sweep, main-analysis
  marker, labels, dimensions, output path, and 300-dpi export.

## Generated artifacts

Regenerate these existing v2 sources in place:

- `diabetes/figures/mn_firing_rate_comparison_combined_v2.png`
- `diabetes/figures/selection_threshold_sensitivity_v2.png`

The Google Docs connector previously rejected local-image binding before any
mutation. The corrected local sources will therefore remain the authoritative
images for manual replacement of the embedded Figures 2 and 3 unless the
connector gains a working private-image write path.

## Verification

1. Add focused regression tests that exercise Figure 2's axis and summary
   artists on a real Matplotlib axis, including their colors, marker shape,
   relative marker size, and draw order.
2. Retain and run the Figure 3 structural checks proving that no lowest-rate or
   eligibility-only comparison remains.
3. Compile `analysis_v2.py` and run `marimo check`.
4. Regenerate both v2 figures without changing the saved numerical results.
5. Inspect both PNGs at original resolution, confirming that Figure 2 uses the
   8–18 pps range without clipping and Figure 3 contains only the randomized
   median, seed band, and thicker green truth reference.
6. Run `git diff --check` and confirm that `diabetes/analysis.py` is unchanged
   from the task baseline.
